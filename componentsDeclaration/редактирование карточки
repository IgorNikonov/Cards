по нажатию кнопки "редактировать", вызывается ф-ция changeDoctor({target}, id)
в файле changeDoctor.js  (стр. 13).  / Если в нее передается вторым параметром id,
то система -в режиме редактирования, если id =null, тогда мы в режиме заполнения новой формы. /

Далее, в завис-ти от типа доктора, в строках 27б 36 или 45 вызывается ф-ция
function fillFormFromCard(id) {    /см.стр. 7/
В ней заполняем инпут-поля формы заполняем данными из объекта получ. из БД.